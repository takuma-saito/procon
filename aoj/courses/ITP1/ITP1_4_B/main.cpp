#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
    cout << fixed << std::setprecision(20);
    double r;
    cin >> r;
    cout << r*r*M_PI << " " << 2*M_PI*r << endl;
}