#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define MAX_N 400
int dp[MAX_N];

int main()
{
    int A,B,C;
    cin >> A;
    cin >> B;
    cin >> C;
    // rep(i, MAX_N) {
    //     dp[i] += dp[i-10][a-1][b][c];
    //     dp[i] += dp[i-5][a][b-1][c];
    //     dp[i] += dp[i-1][a][b][c-1];
    // }
}