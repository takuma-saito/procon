#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
    int a, b, c;
    string s;
    cin >> a;
    cin >> b >> c;
    cin >> s;
    cout << (a+b+c) << " " << s << endl;
}